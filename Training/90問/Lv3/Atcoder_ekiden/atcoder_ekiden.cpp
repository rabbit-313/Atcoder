#include <iostream>
#include <vector>
using namespace std;

int main(){
    int N;
    cin >> N;
    vector <vector<int> > A(N, vector<int>A);
    vector <int> X(M);
    vector <int> Y(M);

    for(int i=0; i<N; i++){
        for(int j=0; j<N; j++){
            cin >> A.at(i).at(j)
        }
    }

    int M;
    cin >> M;
    for(int i=0; i<M; i++){
        cin >> X.at(i) >> Y.at(i);
    }

}